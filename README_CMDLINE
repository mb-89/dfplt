usage: dfplt [-?] [--nonblock] [-v] [--examples] [srcs ...]

Package that parses given glob-style paths and plots any found dataframes.

positional arguments:
  srcs           glob-style paths that will be parsed

optional arguments:
  -?             show this help message and exit
  --nonblock     set to true to open figures in nonblocking mode for debugging or batch exports.
  -v, --version  prints version
  --examples     shows the example gallery
